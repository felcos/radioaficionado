# Copia este archivo a .env (ignorado por git) y define los valores reales.
# Usado por docker-compose.yml.

# Contraseña de PostgreSQL para el contenedor de base de datos.
DB_PASSWORD=cambia-esta-contrasena
